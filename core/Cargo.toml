[package]
name = "viendesu-core"
version = "0.1.0"
edition = "2024"

[features]
default = []
tokio = ["dep:tokio"]
test-util = ["dep:tokio"]

[dependencies]
viendesu-protocol.workspace = true

eva.workspace = true

tokio = { workspace = true, features = ["rt"], optional = true }
